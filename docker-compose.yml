services:
  video-site-91:
    image: ghcr.io/kimjioo/91:latest  # 已经完全指向你自己的专属镜像
    container_name: video-site-91
    restart: always
    ports:
      - "9191:9191"
    volumes:
      - ./data:/opt/video-site-91/data # 视频和数据库等持久化数据存放在这里
    environment:
      - FRONTEND_PORT=9191
